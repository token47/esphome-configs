# For now this file is just annotations and not tested at all

esp32_ble_tracker:
  scan_parameters:
    interval: 1100ms
    window: 1100ms
    active: true

bluetooth_proxy:
  active: true
  cache_services: no

# in particular, Improv needs wifi enabled (wifi auth via BLE)
esp32_improv:
  authorizer: <binary_sensor_id>
